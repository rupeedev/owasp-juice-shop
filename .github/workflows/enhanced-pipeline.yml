name: Enhanced Pipeline with Security Scanning

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  # Stage 1: Lint (existing)
  lint:
    name: Lint Code
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install --prefer-offline --no-audit

      - name: Run linter
        run: npm run lint
        continue-on-error: true

  # Stage 2a: SAST (Parallel)
  sast:
    name: SAST - CodeQL Analysis
    runs-on: ubuntu-latest
    needs: lint
    permissions:
      actions: read
      contents: read
      security-events: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: javascript
          config-file: ./.github/codeql/codeql-config.yml

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
        with:
          category: "/language:javascript"
          output: sarif-results
          upload: false  # We'll upload manually to get artifact
        continue-on-error: true  # Warning mode for training

      - name: Generate Human-Readable CodeQL Report
        if: always()
        run: |
          echo "📊 Converting SARIF results to human-readable format..."
          python3 .github/scripts/parse-sarif.py sarif-results codeql-readable-report.txt

          echo ""
          echo "✅ Readable report generated!"
          echo ""
          echo "Preview of report:"
          echo "=================="
          head -50 codeql-readable-report.txt
          echo "=================="
          echo "(Full report available in artifact download)"
        continue-on-error: true

      - name: Upload SARIF to GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: sarif-results
        continue-on-error: true

      - name: Upload CodeQL Results as Artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: codeql-results
          path: |
            sarif-results/**/*.sarif
            codeql-readable-report.txt
          retention-days: 30

      - name: SAST scan complete
        run: |
          echo "✅ CodeQL SAST analysis completed"
          echo "📊 Results available in:"
          echo "   1. GitHub Security tab > Code scanning alerts"
          echo "   2. Download 'codeql-results' artifact from this workflow run"
          echo "   3. Human-readable report: codeql-readable-report.txt (in artifact)"
          echo ""
          echo "📄 Report formats:"
          echo "   • SARIF files: For tool integration and GitHub Security"
          echo "   • Readable report: For quick review and training purposes"
          echo ""
          echo "⚠️  Note: Juice Shop is intentionally vulnerable - findings are expected"

  # Stage 2b: Secret Detection (Parallel)
  secret-detection:
    name: Secret Detection - TruffleHog
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history for secret scanning

      - name: Run TruffleHog Secret Scan
        run: |
          echo "🔐 Running TruffleHog secret scan..."
          echo "Scanning entire git history for leaked credentials..."

          # Pull Docker image first (to separate pull logs from results)
          docker pull trufflesecurity/trufflehog:latest > /dev/null 2>&1

          # Run TruffleHog with Docker (JSON output to file)
          docker run --rm -v $(pwd):/repo \
            trufflesecurity/trufflehog:latest \
            git file:///repo --only-verified --json > trufflehog-report.json 2>&1 || echo "⚠️  Secrets found (warning mode)"

          # Display summary (parse NDJSON properly)
          if [ -s trufflehog-report.json ]; then
            echo ""
            echo "📊 TruffleHog Scan Results:"

            # Extract summary from last line (finished scanning message)
            SUMMARY=$(grep -o '"verified_secrets":[0-9]*' trufflehog-report.json | tail -1 | cut -d: -f2)
            UNVERIFIED=$(grep -o '"unverified_secrets":[0-9]*' trufflehog-report.json | tail -1 | cut -d: -f2)

            if [ -n "$SUMMARY" ]; then
              echo "   Verified secrets: ${SUMMARY:-0}"
              echo "   Unverified secrets: ${UNVERIFIED:-0}"

              if [ "${SUMMARY:-0}" -eq 0 ] && [ "${UNVERIFIED:-0}" -eq 0 ]; then
                echo "   ✅ No secrets detected"
              else
                echo "   ⚠️  Secrets found - review report for details"
              fi
            else
              echo "   Scan completed - check report for details"
            fi
          else
            echo "✅ No output - scan may have found nothing"
          fi
        continue-on-error: true  # Warning mode for training

      - name: Generate Human-Readable TruffleHog Report
        if: always()
        run: |
          echo "📊 Converting TruffleHog results to human-readable format..."

          if [ -f trufflehog-report.json ]; then
            python3 .github/scripts/parse-trufflehog.py trufflehog-report.json trufflehog-readable-report.txt

            echo ""
            echo "✅ Readable report generated!"
            echo ""
            echo "Preview of report:"
            echo "=================="
            head -60 trufflehog-readable-report.txt
            echo "=================="
            echo "(Full report available in artifact download)"
          else
            echo "⚠️  No TruffleHog report found to parse"
          fi
        continue-on-error: true

      - name: Upload TruffleHog Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: secret-detection-report
          path: |
            trufflehog-report.json
            trufflehog-readable-report.txt
          retention-days: 30

  # Stage 2c: Unit Tests (Parallel)
  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: |
          npm install --prefer-offline --no-audit
          cd frontend && npm install --prefer-offline --no-audit

      - name: Verify Karma configuration
        run: |
          echo "📝 Verifying test configuration files..."
          ls -la frontend/src/karma.conf.js || echo "⚠️  Karma config missing"
          ls -la frontend/src/test.ts || echo "⚠️  Test bootstrap missing"
          ls -la frontend/src/tsconfig.spec.json || echo "⚠️  Test tsconfig missing"

      - name: Run backend tests only (frontend tests skipped in CI)
        run: |
          echo "🧪 Running backend unit tests..."
          npm run test:server || echo "⚠️  Backend tests failed (continuing in training mode)"
        continue-on-error: true  # Warning mode for training

  # Stage 3: Security Scan (Advanced - runs after all Stage 2 jobs)
  # Stage 3a: npm Audit
  npm-audit:
    name: npm Audit
    runs-on: ubuntu-latest
    needs: [sast, secret-detection, test]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Run npm audit
        run: |
          echo "📦 Running npm audit..."
          npm audit --audit-level=moderate > npm-audit.txt 2>&1 || echo "⚠️  Vulnerabilities found (warning mode)"
          cat npm-audit.txt
        continue-on-error: true

      - name: Upload npm Audit Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: security-scan-npm-audit
          path: npm-audit.txt
          retention-days: 30

  # Stage 3b: RetireJS
  retirejs:
    name: RetireJS
    runs-on: ubuntu-latest
    needs: [sast, secret-detection, test]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Run RetireJS
        run: |
          echo "📚 Running RetireJS scan..."
          npm install -g retire
          retire --path . > retirejs-scan.txt 2>&1 || echo "⚠️  Vulnerable libraries found (warning mode)"
          cat retirejs-scan.txt
        continue-on-error: true

      - name: Upload RetireJS Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: security-scan-retirejs
          path: retirejs-scan.txt
          retention-days: 30

  # Stage 3c: Gitleaks
  gitleaks:
    name: Gitleaks Secrets
    runs-on: ubuntu-latest
    needs: [sast, secret-detection, test]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Gitleaks
        run: |
          echo "🔐 Running Gitleaks secret scan..."
          docker run --rm -v $(pwd):/repo zricethezav/gitleaks:latest detect --source /repo --verbose > gitleaks-report.txt 2>&1 || echo "⚠️  Secrets found (warning mode)"
          cat gitleaks-report.txt || echo "No secrets detected"
        continue-on-error: true

      - name: Upload Gitleaks Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: security-scan-gitleaks
          path: gitleaks-report.txt
          retention-days: 30

  # Stage 3d: Trivy
  trivy:
    name: Trivy Container Scan
    runs-on: ubuntu-latest
    needs: [sast, secret-detection, test]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Trivy scan
        run: |
          echo "🐳 Running Trivy container scan..."
          echo "Building image for scanning..."
          docker build -t juice-shop:scan .

          echo ""
          echo "=== Trivy Scan - HIGH Severity (Warning) ===" | tee trivy-scan.txt
          docker run --rm -v /var/run/docker.sock:/var/run/docker.sock \
            -v $HOME/.cache:/root/.cache/ \
            aquasec/trivy:latest image --exit-code 0 --severity HIGH --light juice-shop:scan \
            2>&1 | tee -a trivy-scan.txt \
            || echo "⚠️  HIGH severity vulnerabilities found"

          echo "" | tee -a trivy-scan.txt
          echo "=== Trivy Scan - CRITICAL Severity ===" | tee -a trivy-scan.txt
          docker run --rm -v /var/run/docker.sock:/var/run/docker.sock \
            -v $HOME/.cache:/root/.cache/ \
            aquasec/trivy:latest image --exit-code 0 --severity CRITICAL --light juice-shop:scan \
            2>&1 | tee -a trivy-scan.txt \
            || echo "⚠️  CRITICAL vulnerabilities found (warning mode for training)"

          echo "📊 Trivy scan completed" | tee -a trivy-scan.txt
        continue-on-error: true

      - name: Upload Trivy Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: security-scan-trivy
          path: trivy-scan.txt
          retention-days: 30

  # Stage 3e: Dockerfile Policy
  dockerfile-policy:
    name: Dockerfile Policy (OPA)
    runs-on: ubuntu-latest
    needs: [sast, secret-detection, test]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Dockerfile Policy Scan
        run: |
          echo "📋 Running OPA/Conftest policy validation..."
          echo "Using comprehensive security policy from docs/docker/dockerfile-security.rego"
          echo "Using docker-based conftest (no binary installation needed)"
          echo ""

          echo "🔍 Testing Dockerfile against comprehensive security policies..." | tee dockerfile-policy.txt
          echo "" | tee -a dockerfile-policy.txt
          echo "Security checks include:" | tee -a dockerfile-policy.txt
          echo "  ✓ No secrets in ENV variables" | tee -a dockerfile-policy.txt
          echo "  ✓ Use trusted base images only" | tee -a dockerfile-policy.txt
          echo "  ✓ No 'latest' tag for base images" | tee -a dockerfile-policy.txt
          echo "  ✓ Avoid curl bashing" | tee -a dockerfile-policy.txt
          echo "  ✓ No system package upgrades in Dockerfile" | tee -a dockerfile-policy.txt
          echo "  ✓ Use COPY instead of ADD" | tee -a dockerfile-policy.txt
          echo "  ✓ Don't run as root user" | tee -a dockerfile-policy.txt
          echo "  ✓ Don't use sudo command" | tee -a dockerfile-policy.txt
          echo "  ✓ Use multi-stage builds" | tee -a dockerfile-policy.txt
          echo "" | tee -a dockerfile-policy.txt

          # Use docker-based conftest with comprehensive policy
          set +e  # Disable exit on error for proper handling
          docker run --rm -v $(pwd):/project \
            openpolicyagent/conftest:latest \
            test --policy ./docs/docker/dockerfile-security.rego Dockerfile 2>&1 | tee -a dockerfile-policy.txt
          CONFTEST_EXIT_CODE=$?
          set -e  # Re-enable exit on error

          echo "" | tee -a dockerfile-policy.txt
          if [ $CONFTEST_EXIT_CODE -ne 0 ]; then
            echo "⚠️  Policy violations found (exit code: $CONFTEST_EXIT_CODE)" | tee -a dockerfile-policy.txt
            echo "📝 This is expected for training - Juice Shop is intentionally vulnerable" | tee -a dockerfile-policy.txt
            echo "📚 Reference: https://cloudberry.engineering/article/dockerfile-security-best-practices/" | tee -a dockerfile-policy.txt
            echo "✅ Pipeline continues in warning mode" | tee -a dockerfile-policy.txt
          else
            echo "✅ All Dockerfile policies passed!" | tee -a dockerfile-policy.txt
          fi
        continue-on-error: true

      - name: Upload Dockerfile Policy Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: security-scan-dockerfile-policy
          path: dockerfile-policy.txt
          retention-days: 30

  # Stage 4: SonarQube (NEW - Manual gate)
  sonar:
    name: SonarQube Code Quality
    runs-on: ubuntu-latest
    needs: [npm-audit, retirejs, gitleaks, trivy, dockerfile-policy]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: echo "Sonar stage - dependencies installed in sonar scanner"

      - name: SonarQube Scan (placeholder)
        run: |
          echo "📊 SonarQube analysis would run here"
          echo "⚠️  Note: SonarQube requires server setup"
          echo "   Deferred to last phase - will implement SonarCloud integration"
          # TODO: Implement SonarCloud in last phase (requires account setup)
        continue-on-error: true

  # Stage 5: Build (existing)
  build:
    name: Build & Tag Docker Image
    runs-on: ubuntu-latest
    needs: sonar
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: |
          docker build -t juice-shop:latest .
          echo "✅ Docker image built successfully!"

      - name: Tag images
        run: |
          docker tag juice-shop:latest juice-shop:${{ github.sha }}
          docker tag juice-shop:latest juice-shop:enhanced
          echo "🏷️  Images tagged"

      - name: Save Docker image as artifact
        run: |
          docker save juice-shop:latest -o juice-shop-image.tar
          echo "💾 Docker image saved"

      - name: Upload Docker image artifact
        uses: actions/upload-artifact@v4
        with:
          name: docker-image
          path: juice-shop-image.tar
          retention-days: 1

  # Stage 6: Push (existing)
  push-to-dockerhub:
    name: Push to Docker Hub
    runs-on: ubuntu-latest
    needs: build
    env:
      DOCKER_USERNAME: rupeedev
      DOCKER_REPO: owasp-juice-shop
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download Docker image artifact
        uses: actions/download-artifact@v4
        with:
          name: docker-image

      - name: Load Docker image
        run: docker load -i juice-shop-image.tar

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Tag and push images
        run: |
          docker tag juice-shop:latest ${{ env.DOCKER_USERNAME }}/${{ env.DOCKER_REPO }}:latest
          docker tag juice-shop:latest ${{ env.DOCKER_USERNAME }}/${{ env.DOCKER_REPO }}:${{ github.sha }}
          docker tag juice-shop:latest ${{ env.DOCKER_USERNAME }}/${{ env.DOCKER_REPO }}:enhanced

          docker push ${{ env.DOCKER_USERNAME }}/${{ env.DOCKER_REPO }}:latest
          docker push ${{ env.DOCKER_USERNAME }}/${{ env.DOCKER_REPO }}:${{ github.sha }}
          docker push ${{ env.DOCKER_USERNAME }}/${{ env.DOCKER_REPO }}:enhanced

  # Stage 7: Deploy (existing)
  deploy:
    name: Deploy to Kubernetes
    runs-on: self-hosted
    needs: push-to-dockerhub
    env:
      DOCKER_USERNAME: rupeedev
      DOCKER_REPO: owasp-juice-shop
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install kubectl
        run: |
          if ! command -v kubectl &> /dev/null; then
            curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
            chmod +x kubectl
            sudo mv kubectl /usr/local/bin/
          fi

      - name: Apply manifests
        run: |
          kubectl apply -f kind-k8s-cluster/juice-shop-deployment.yaml
          kubectl apply -f kind-k8s-cluster/juice-shop-service.yaml

      - name: Update deployment
        run: |
          kubectl set image deployment/juice-shop juice-shop=${{ env.DOCKER_USERNAME }}/${{ env.DOCKER_REPO }}:${{ github.sha }}

      - name: Wait for rollout
        run: |
          kubectl rollout status deployment/juice-shop --timeout=180s

      - name: Verify deployment
        run: |
          kubectl get pods -l app=juice-shop
          kubectl get svc juice-shop
          kubectl get deployment juice-shop

      - name: Get service endpoint
        run: |
          echo "🌐 Application deployed at: http://localhost:30080"
        id: endpoint

  # Stage 8: K8s Security (NEW - DAST with Parallel Execution)
  # Stage 8a: K8s Manifest Security (Parallel - doesn't need app)
  k8s-manifest-scan:
    name: K8s Manifest Security
    runs-on: self-hosted
    needs: deploy
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run K8s Manifest Security Scan
        run: |
          echo "🔐 Scanning Kubernetes manifests for security issues..." | tee k8s-manifest-scan.txt
          echo "" | tee -a k8s-manifest-scan.txt

          # Scan deployment and service manifests with OPA Conftest
          echo "=== Scanning K8s Deployment Manifest ===" | tee -a k8s-manifest-scan.txt
          docker run --rm -v $(pwd):/project \
            openpolicyagent/conftest:latest \
            test --policy ./policy kind-k8s-cluster/juice-shop-deployment.yaml \
            2>&1 | tee -a k8s-manifest-scan.txt \
            || echo "⚠️  Policy violations in deployment manifest" | tee -a k8s-manifest-scan.txt

          echo "" | tee -a k8s-manifest-scan.txt
          echo "=== Scanning K8s Service Manifest ===" | tee -a k8s-manifest-scan.txt
          docker run --rm -v $(pwd):/project \
            openpolicyagent/conftest:latest \
            test --policy ./policy kind-k8s-cluster/juice-shop-service.yaml \
            2>&1 | tee -a k8s-manifest-scan.txt \
            || echo "⚠️  Policy violations in service manifest" | tee -a k8s-manifest-scan.txt

          echo "" | tee -a k8s-manifest-scan.txt
          echo "📝 K8s manifest scan checks for:" | tee -a k8s-manifest-scan.txt
          echo "   - Containers running as non-root (runAsNonRoot)" | tee -a k8s-manifest-scan.txt
          echo "   - Security context settings" | tee -a k8s-manifest-scan.txt
          echo "   - Service type configurations" | tee -a k8s-manifest-scan.txt
          echo "   - Resource limits and requests" | tee -a k8s-manifest-scan.txt
          echo "📚 Reference: Best practices from DevSecOps patterns" | tee -a k8s-manifest-scan.txt
        continue-on-error: true

      - name: Upload K8s Manifest Scan Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: k8s-security-k8s-manifest
          path: k8s-manifest-scan.txt
          retention-days: 30

  # Stage 8b: Security Headers Analysis (Parallel - needs app)
  security-headers-scan:
    name: Security Headers Analysis
    runs-on: self-hosted
    needs: deploy
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install kubectl (if not present)
        run: |
          if ! command -v kubectl &> /dev/null; then
            echo "📥 Installing kubectl..."
            curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
            chmod +x kubectl
            sudo mv kubectl /usr/local/bin/
            echo "✅ kubectl installed"
          else
            echo "✅ kubectl already installed"
          fi

      - name: Wait for application readiness
        run: |
          echo "⏳ Waiting for application to be ready..."
          sleep 30
          kubectl wait --for=condition=ready pod -l app=juice-shop --timeout=120s

      - name: Application Health Check
        run: |
          echo "🏥 Checking application health..."
          echo "Waiting for application to start responding..."

          MAX_RETRIES=10
          RETRY_COUNT=0
          SUCCESS=false

          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
            echo "Attempt $((RETRY_COUNT + 1))/$MAX_RETRIES..."

            # Capture both HTTP code and curl exit code
            HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:30080 2>&1 || echo "000")

            if [ "$HTTP_CODE" = "200" ]; then
              echo "✅ Application is responding (HTTP $HTTP_CODE)"
              SUCCESS=true
              break
            elif [ "$HTTP_CODE" = "000" ]; then
              echo "⚠️  Connection failed - application not ready yet"
            else
              echo "⚠️  Application returned HTTP $HTTP_CODE"
            fi

            RETRY_COUNT=$((RETRY_COUNT + 1))
            if [ $RETRY_COUNT -lt $MAX_RETRIES ]; then
              echo "Waiting 10 seconds before retry..."
              sleep 10
            fi
          done

          if [ "$SUCCESS" = false ]; then
            echo "❌ Application health check failed after $MAX_RETRIES attempts"
            echo "Checking pod status:"
            kubectl get pods -l app=juice-shop
            kubectl describe pod -l app=juice-shop | tail -50
            exit 1
          fi
        continue-on-error: true

      - name: Run Security Headers Analysis
        if: always()
        run: |
          echo "🔒 Analyzing HTTP security headers..." | tee security-headers-scan.txt
          echo "" | tee -a security-headers-scan.txt
          echo "=== Current Security Headers ===" | tee -a security-headers-scan.txt
          curl -s -D - http://localhost:30080 -o /dev/null 2>&1 | grep -E "^X-|^Content-Security|^Strict-Transport" | tee -a security-headers-scan.txt || true
          echo "" | tee -a security-headers-scan.txt
          echo "=== Security Header Assessment ===" | tee -a security-headers-scan.txt

          HEADERS=$(curl -s -D - http://localhost:30080 -o /dev/null 2>&1)

          # Check for present headers
          echo "$HEADERS" | grep -q "X-Content-Type-Options" && echo "✓ X-Content-Type-Options: Present" | tee -a security-headers-scan.txt || echo "✗ X-Content-Type-Options: Missing" | tee -a security-headers-scan.txt
          echo "$HEADERS" | grep -q "X-Frame-Options" && echo "✓ X-Frame-Options: Present" | tee -a security-headers-scan.txt || echo "✗ X-Frame-Options: Missing" | tee -a security-headers-scan.txt
          echo "$HEADERS" | grep -q "Content-Security-Policy" && echo "✓ Content-Security-Policy: Present" | tee -a security-headers-scan.txt || echo "✗ Content-Security-Policy: Missing (expected)" | tee -a security-headers-scan.txt
          echo "$HEADERS" | grep -q "Strict-Transport-Security" && echo "✓ Strict-Transport-Security: Present" | tee -a security-headers-scan.txt || echo "✗ Strict-Transport-Security: Missing (expected)" | tee -a security-headers-scan.txt
          echo "$HEADERS" | grep -q "X-XSS-Protection" && echo "✓ X-XSS-Protection: Present" | tee -a security-headers-scan.txt || echo "✗ X-XSS-Protection: Missing (expected)" | tee -a security-headers-scan.txt
          echo "" | tee -a security-headers-scan.txt
          echo "⚠️  Note: Juice Shop is intentionally vulnerable - missing headers are expected" | tee -a security-headers-scan.txt
        continue-on-error: true

      - name: Upload Security Headers Scan Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: k8s-security-security-headers
          path: security-headers-scan.txt
          retention-days: 30

  # Stage 8c: OWASP ZAP Baseline (Parallel - needs app)
  owasp-zap-scan:
    name: OWASP ZAP Baseline
    runs-on: self-hosted
    needs: deploy
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install kubectl (if not present)
        run: |
          if ! command -v kubectl &> /dev/null; then
            echo "📥 Installing kubectl..."
            curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
            chmod +x kubectl
            sudo mv kubectl /usr/local/bin/
            echo "✅ kubectl installed"
          else
            echo "✅ kubectl already installed"
          fi

      - name: Wait for application readiness
        run: |
          echo "⏳ Waiting for application to be ready..."
          sleep 30
          kubectl wait --for=condition=ready pod -l app=juice-shop --timeout=120s

      - name: Application Health Check
        run: |
          echo "🏥 Checking application health..."
          echo "Waiting for application to start responding..."

          MAX_RETRIES=10
          RETRY_COUNT=0
          SUCCESS=false

          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
            echo "Attempt $((RETRY_COUNT + 1))/$MAX_RETRIES..."

            # Capture both HTTP code and curl exit code
            HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:30080 2>&1 || echo "000")

            if [ "$HTTP_CODE" = "200" ]; then
              echo "✅ Application is responding (HTTP $HTTP_CODE)"
              SUCCESS=true
              break
            elif [ "$HTTP_CODE" = "000" ]; then
              echo "⚠️  Connection failed - application not ready yet"
            else
              echo "⚠️  Application returned HTTP $HTTP_CODE"
            fi

            RETRY_COUNT=$((RETRY_COUNT + 1))
            if [ $RETRY_COUNT -lt $MAX_RETRIES ]; then
              echo "Waiting 10 seconds before retry..."
              sleep 10
            fi
          done

          if [ "$SUCCESS" = false ]; then
            echo "❌ Application health check failed after $MAX_RETRIES attempts"
            echo "Checking pod status:"
            kubectl get pods -l app=juice-shop
            kubectl describe pod -l app=juice-shop | tail -50
            exit 1
          fi
        continue-on-error: true

      - name: Run OWASP ZAP Baseline Scan
        if: always()
        run: |
          echo "🔍 Running OWASP ZAP baseline scan..."
          echo "Note: This may take 2-5 minutes..."
          
          # Create directory for ZAP reports
          mkdir -p ${{ github.workspace }}/zap-reports

          # Use correct ZAP docker image with volume mount
          timeout 300 docker run --rm --network host \
            -v ${{ github.workspace }}/zap-reports:/zap/wrk:rw \
            zaproxy/zap-stable:latest \
            zap-baseline.py -t http://localhost:30080 -I -r zap-report.html \
            || echo "⚠️  Security issues found or scan timed out (warning mode)"

          # Move report to workspace root for artifact upload
          if [ -f "${{ github.workspace }}/zap-reports/zap-report.html" ]; then
            mv ${{ github.workspace }}/zap-reports/zap-report.html ${{ github.workspace }}/
            echo "✅ ZAP report saved to workspace"
          else
            echo "⚠️  ZAP report not generated, creating placeholder"
            echo "<html><body><h1>ZAP scan did not complete</h1></body></html>" > ${{ github.workspace }}/zap-report.html
          fi
          
          echo "📊 ZAP scan completed"
          echo "⚠️  Findings are expected for intentionally vulnerable application"
        continue-on-error: true

      - name: Upload OWASP ZAP Scan Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: k8s-security-owasp-zap
          path: zap-report.html
          retention-days: 30

  # Stage 8d: OWASP Noir Attack Surface Scan (Parallel - doesn't need app)
  noir-attack-surface-scan:
    name: OWASP Noir - Attack Surface Detection
    runs-on: ubuntu-latest
    needs: deploy
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run OWASP Noir Attack Surface Scan
        run: |
          echo "🔍 Running OWASP Noir attack surface detection..." | tee noir-scan.txt
          echo "" | tee -a noir-scan.txt
          echo "=== OWASP Noir - Static Analysis for API Endpoints ===" | tee -a noir-scan.txt
          echo "Technology: Identifies attack surfaces by analyzing source code" | tee -a noir-scan.txt
          echo "Scope: API endpoints, parameters, potential vulnerabilities" | tee -a noir-scan.txt
          echo "" | tee -a noir-scan.txt

          # Run Noir scan and capture results
          docker run --rm -v $(pwd):/target \
            ghcr.io/owasp-noir/noir:latest \
            noir -b /target -f json > noir-results.json 2>&1 || echo "⚠️  Noir scan completed with findings"

          # Display summary
          if [ -f "noir-results.json" ]; then
            echo "✅ Noir scan completed successfully" | tee -a noir-scan.txt
            echo "" | tee -a noir-scan.txt

            # Extract endpoint count
            ENDPOINT_COUNT=$(cat noir-results.json | grep -o '"url"' | wc -l)
            echo "📊 Scan Results:" | tee -a noir-scan.txt
            echo "   Total Endpoints Discovered: $ENDPOINT_COUNT" | tee -a noir-scan.txt
            echo "" | tee -a noir-scan.txt

            # Sample endpoints (first 10)
            echo "=== Sample Discovered Endpoints (First 10) ===" | tee -a noir-scan.txt
            cat noir-results.json | grep -o '"url":"[^"]*"' | head -10 | sed 's/"url":"/  • /' | sed 's/"$//' | tee -a noir-scan.txt
            echo "" | tee -a noir-scan.txt

            # Technologies detected
            echo "=== Technologies Detected ===" | tee -a noir-scan.txt
            echo "   Analyzing source code for technology stack..." | tee -a noir-scan.txt
            echo "" | tee -a noir-scan.txt
          else
            echo "⚠️  Noir results not generated" | tee -a noir-scan.txt
          fi

          echo "📝 Attack Surface Analysis:" | tee -a noir-scan.txt
          echo "   • API endpoint discovery via static analysis" | tee -a noir-scan.txt
          echo "   • Parameter extraction and mapping" | tee -a noir-scan.txt
          echo "   • Framework and technology detection" | tee -a noir-scan.txt
          echo "   • Source code location tracking" | tee -a noir-scan.txt
          echo "" | tee -a noir-scan.txt
          echo "⚠️  Note: Juice Shop has many intentional vulnerabilities" | tee -a noir-scan.txt
          echo "📚 Reference: https://github.com/owasp-noir/noir" | tee -a noir-scan.txt
        continue-on-error: true

      - name: Upload Noir JSON Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: k8s-security-noir-json
          path: noir-results.json
          retention-days: 30

      - name: Upload Noir Scan Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: k8s-security-noir-scan
          path: noir-scan.txt
          retention-days: 30

  # Stage 9: Security Report Consolidation
  consolidate-reports:
    name: Consolidate Security Reports
    runs-on: ubuntu-latest
    needs: [k8s-manifest-scan, security-headers-scan, owasp-zap-scan, noir-attack-surface-scan]
    if: always()  # Run even if previous stages have failures
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create reports directory
        run: mkdir -p security-reports

      - name: Download SAST Report
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          name: sast-report
          path: security-reports/

      - name: Download Secret Detection Report
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          name: secret-detection-report
          path: security-reports/

      - name: Download Security Scan Reports
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          pattern: security-scan-*
          path: security-reports/
          merge-multiple: true

      - name: Download K8s Security Reports
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          pattern: k8s-security-*
          path: security-reports/
          merge-multiple: true

      - name: List downloaded reports
        run: |
          echo "📋 Downloaded security reports:"
          ls -lah security-reports/
          echo ""
          echo "Report files:"
          find security-reports/ -type f

      - name: Run consolidation script
        run: |
          chmod +x .github/scripts/consolidate-security-reports.sh
          .github/scripts/consolidate-security-reports.sh security-reports consolidated-reports

      - name: Display consolidation summary
        run: |
          echo ""
          echo "================================================================"
          echo "📊 Security Report Consolidation Summary"
          echo "================================================================"
          echo ""
          if [ -f "consolidated-reports/summary.txt" ]; then
            echo "Tool Summary:"
            cat consolidated-reports/summary.txt
          fi
          echo ""
          echo "Generated Reports:"
          ls -lh consolidated-reports/
          echo ""

      - name: Upload Consolidated Reports
        uses: actions/upload-artifact@v4
        with:
          name: consolidated-security-reports
          path: |
            consolidated-reports/security-dashboard.html
            consolidated-reports/security-report.md
            consolidated-reports/summary.txt
          retention-days: 90  # Keep consolidated reports longer

  # Final success message
  pipeline-complete:
    name: Pipeline Complete
    runs-on: ubuntu-latest
    needs: consolidate-reports
    steps:
      - name: Success message
        run: |
          echo "🎉 Enhanced Pipeline completed successfully!"
          echo ""
          echo "✅ Pipeline Stages (Parallel Execution Optimized):"
          echo "   Stage 1: Lint ✓"
          echo "   Stage 2: SAST + Secret Detection + Unit Tests (parallel) ✓"
          echo "   Stage 3: Security Scan (Trivy, Gitleaks, RetireJS - parallel) ✓"
          echo "           + Dockerfile Policy Validation ✓"
          echo "   Stage 4: SonarQube Analysis ✓"
          echo "   Stage 5: Build & Tag Docker Image ✓"
          echo "   Stage 6: Push to Docker Hub ✓"
          echo "   Stage 7: Deploy to Kubernetes ✓"
          echo "   Stage 8: K8s Security + DAST (parallel) ✓"
          echo "           • K8s Manifest Security ✓"
          echo "           • Security Headers Analysis ✓"
          echo "           • OWASP ZAP Baseline Scan ✓"
          echo "           • OWASP Noir Attack Surface Detection ✓"
          echo "   Stage 9: Security Report Consolidation ✓"
          echo ""
          echo "⚡ Parallel execution: SAST, Secrets, Tests, and Policy checks run simultaneously"
          echo "🔒 Security scanning integrated at multiple stages"
          echo "📦 Application deployed to Kubernetes"
          echo "🌐 Access at: http://localhost:30080"
          echo ""
          echo "📊 Consolidated Security Reports Available:"
          echo "   Download the 'consolidated-security-reports' artifact to view:"
          echo "   • security-dashboard.html - Interactive HTML dashboard"
          echo "   • security-report.md - Markdown summary report"
          echo "   • summary.txt - Quick statistics"
