name: Enhanced Pipeline with Security Scanning

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  # Stage 1: Lint (existing)
  lint:
    name: Lint Code
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install --prefer-offline --no-audit

      - name: Run linter
        run: npm run lint
        continue-on-error: true

  # Stage 2a: SAST (Parallel)
  sast:
    name: SAST - Static Analysis
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install --prefer-offline --no-audit

      - name: Run SAST (npm audit)
        run: |
          echo "🔍 Running SAST scan..."
          # TODO: Implement CodeQL or Semgrep
          # For now, use npm audit as basic SAST
          npm audit --audit-level=high || echo "⚠️  Vulnerabilities found (warning mode)"
        continue-on-error: true  # Warning mode for training

  # Stage 2b: Secret Detection (Parallel)
  secret-detection:
    name: Secret Detection
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history for secret scanning

      - name: Run Secret Detection
        run: |
          echo "🔐 Scanning for secrets in git history..."
          # TODO: Implement TruffleHog or detect-secrets
          # For now, use basic git secrets check
          git log --all --pretty=format: --name-only | sort -u | xargs grep -i "password\|secret\|key" || echo "✅ No obvious secrets found"
        continue-on-error: true  # Warning mode for training

  # Stage 2c: Unit Tests (Parallel)
  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install --prefer-offline --no-audit

      - name: Run unit tests
        run: npm test
        continue-on-error: true  # Warning mode for training

  # Stage 3: Security Scan (Advanced - runs after all Stage 2 jobs)
  security-scan:
    name: Security Vulnerability Scanning
    runs-on: ubuntu-latest
    needs: [sast, secret-detection, test]
    strategy:
      fail-fast: false  # Run all scans even if one fails
      matrix:
        scan:
          - name: npm-audit
            tool: npm
          - name: trivy
            tool: trivy
          - name: gitleaks
            tool: gitleaks
          - name: retirejs
            tool: retire

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
        if: matrix.scan.name == 'npm-audit' || matrix.scan.name == 'retirejs'

      # npm audit
      - name: Run npm audit
        if: matrix.scan.name == 'npm-audit'
        run: |
          echo "📦 Running npm audit..."
          npm audit --audit-level=moderate || echo "⚠️  Vulnerabilities found (warning mode)"
        continue-on-error: true

      # Trivy container scan
      - name: Run Trivy scan
        if: matrix.scan.name == 'trivy'
        run: |
          echo "🐳 Running Trivy container scan..."
          # Build image first
          docker build -t juice-shop:scan .
          # Install Trivy
          wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key | sudo apt-key add -
          echo "deb https://aquasecurity.github.io/trivy-repo/deb $(lsb_release -sc) main" | sudo tee /etc/apt/sources.list.d/trivy.list
          sudo apt-get update
          sudo apt-get install -y trivy
          # Scan image
          trivy image --severity HIGH,CRITICAL juice-shop:scan || echo "⚠️  Vulnerabilities found (warning mode)"
        continue-on-error: true

      # Gitleaks secret scanning
      - name: Run Gitleaks
        if: matrix.scan.name == 'gitleaks'
        run: |
          echo "🔐 Running Gitleaks secret scan..."
          docker run --rm -v $(pwd):/repo zricethezav/gitleaks:latest detect --source /repo --verbose || echo "⚠️  Secrets found (warning mode)"
        continue-on-error: true

      # RetireJS
      - name: Run RetireJS
        if: matrix.scan.name == 'retirejs'
        run: |
          echo "📚 Running RetireJS scan..."
          npm install -g retire
          retire --path . || echo "⚠️  Vulnerable libraries found (warning mode)"
        continue-on-error: true

  # OPA/Conftest - Dockerfile policy check (can run in parallel with Stage 2)
  dockerfile-policy:
    name: Dockerfile Policy Validation
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run OPA/Conftest
        run: |
          echo "📋 Running OPA/Conftest policy validation..."
          wget -q https://github.com/open-policy-agent/conftest/releases/latest/download/conftest_linux_amd64.tar.gz
          tar xzf conftest_linux_amd64.tar.gz
          sudo mv conftest /usr/local/bin/

          echo "🔍 Testing Dockerfile against security policies..."
          conftest test Dockerfile --policy ./policy || {
            echo "⚠️  Policy violations found (warning mode for training)"
            echo "📝 Review policy violations above to understand security best practices"
            exit 0
          }
        continue-on-error: false

  # Stage 4: SonarQube (NEW - Manual gate)
  sonar:
    name: SonarQube Code Quality
    runs-on: ubuntu-latest
    needs: [security-scan, dockerfile-policy]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install

      - name: SonarQube Scan (placeholder)
        run: |
          echo "📊 SonarQube analysis would run here"
          echo "⚠️  Note: SonarQube requires server setup"
          echo "   For now, this is a placeholder for future integration"
          # TODO: Implement SonarCloud or self-hosted SonarQube
        continue-on-error: true

  # Stage 5: Build (existing)
  build:
    name: Build & Tag Docker Image
    runs-on: ubuntu-latest
    needs: sonar
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: |
          docker build -t juice-shop:latest .
          echo "✅ Docker image built successfully!"

      - name: Tag images
        run: |
          docker tag juice-shop:latest juice-shop:${{ github.sha }}
          docker tag juice-shop:latest juice-shop:enhanced
          echo "🏷️  Images tagged"

      - name: Save Docker image as artifact
        run: |
          docker save juice-shop:latest -o juice-shop-image.tar
          echo "💾 Docker image saved"

      - name: Upload Docker image artifact
        uses: actions/upload-artifact@v4
        with:
          name: docker-image
          path: juice-shop-image.tar
          retention-days: 1

  # Stage 6: Push (existing)
  push-to-dockerhub:
    name: Push to Docker Hub
    runs-on: ubuntu-latest
    needs: build
    env:
      DOCKER_USERNAME: rupeedev
      DOCKER_REPO: owasp-juice-shop
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download Docker image artifact
        uses: actions/download-artifact@v4
        with:
          name: docker-image

      - name: Load Docker image
        run: docker load -i juice-shop-image.tar

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Tag and push images
        run: |
          docker tag juice-shop:latest ${{ env.DOCKER_USERNAME }}/${{ env.DOCKER_REPO }}:latest
          docker tag juice-shop:latest ${{ env.DOCKER_USERNAME }}/${{ env.DOCKER_REPO }}:${{ github.sha }}
          docker tag juice-shop:latest ${{ env.DOCKER_USERNAME }}/${{ env.DOCKER_REPO }}:enhanced

          docker push ${{ env.DOCKER_USERNAME }}/${{ env.DOCKER_REPO }}:latest
          docker push ${{ env.DOCKER_USERNAME }}/${{ env.DOCKER_REPO }}:${{ github.sha }}
          docker push ${{ env.DOCKER_USERNAME }}/${{ env.DOCKER_REPO }}:enhanced

  # Stage 7: Deploy (existing)
  deploy:
    name: Deploy to Kubernetes
    runs-on: self-hosted
    needs: push-to-dockerhub
    env:
      DOCKER_USERNAME: rupeedev
      DOCKER_REPO: owasp-juice-shop
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install kubectl
        run: |
          if ! command -v kubectl &> /dev/null; then
            curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
            chmod +x kubectl
            sudo mv kubectl /usr/local/bin/
          fi

      - name: Apply manifests
        run: |
          kubectl apply -f kind-k8s-cluster/juice-shop-deployment.yaml
          kubectl apply -f kind-k8s-cluster/juice-shop-service.yaml

      - name: Update deployment
        run: |
          kubectl set image deployment/juice-shop juice-shop=${{ env.DOCKER_USERNAME }}/${{ env.DOCKER_REPO }}:${{ github.sha }}

      - name: Wait for rollout
        run: |
          kubectl rollout status deployment/juice-shop --timeout=180s

      - name: Verify deployment
        run: |
          kubectl get pods -l app=juice-shop
          kubectl get svc juice-shop
          kubectl get deployment juice-shop

      - name: Get service endpoint
        run: |
          echo "🌐 Application deployed at: http://localhost:30080"
        id: endpoint

  # Stage 8: K8s Security (NEW - DAST)
  k8s-security:
    name: K8s Security - DAST
    runs-on: self-hosted
    needs: deploy
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Wait for application readiness
        run: |
          echo "⏳ Waiting for application to be ready..."
          sleep 30
          kubectl wait --for=condition=ready pod -l app=juice-shop --timeout=120s

      # OWASP ZAP Baseline Scan
      - name: Run OWASP ZAP Baseline Scan
        run: |
          echo "🔍 Running OWASP ZAP baseline scan..."
          docker run --rm --network host \
            owasp/zap2docker-stable:latest \
            zap-baseline.py -t http://localhost:30080 -r zap-report.html || echo "⚠️  Security issues found (warning mode)"
        continue-on-error: true

      # Security Headers Check
      - name: Check Security Headers
        run: |
          echo "🔒 Checking security headers..."
          curl -I http://localhost:30080 | grep -E "Content-Security-Policy|X-Frame-Options|Strict-Transport-Security|X-Content-Type-Options|X-XSS-Protection" || echo "⚠️  Missing security headers (expected for Juice Shop)"
        continue-on-error: true

      - name: Security scan complete
        run: |
          echo "✅ K8s Security scans completed"
          echo "📊 Review DAST results for security findings"
          echo "⚠️  Note: Juice Shop is intentionally vulnerable - findings are expected"

  # Final success message
  pipeline-complete:
    name: Pipeline Complete
    runs-on: ubuntu-latest
    needs: k8s-security
    steps:
      - name: Success message
        run: |
          echo "🎉 Enhanced Pipeline completed successfully!"
          echo ""
          echo "✅ Pipeline Stages (Parallel Execution Optimized):"
          echo "   Stage 1: Lint ✓"
          echo "   Stage 2: SAST + Secret Detection + Unit Tests (parallel) ✓"
          echo "   Stage 3: Security Scan (Trivy, Gitleaks, RetireJS - parallel) ✓"
          echo "           + Dockerfile Policy Validation ✓"
          echo "   Stage 4: SonarQube Analysis ✓"
          echo "   Stage 5: Build & Tag Docker Image ✓"
          echo "   Stage 6: Push to Docker Hub ✓"
          echo "   Stage 7: Deploy to Kubernetes ✓"
          echo "   Stage 8: K8s Security (DAST with OWASP ZAP) ✓"
          echo ""
          echo "⚡ Parallel execution: SAST, Secrets, Tests, and Policy checks run simultaneously"
          echo "🔒 Security scanning integrated at multiple stages"
          echo "📦 Application deployed to Kubernetes"
          echo "🌐 Access at: http://localhost:30080"
